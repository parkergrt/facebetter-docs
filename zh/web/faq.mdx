---
title: "常见问题"
description: "Facebetter Web SDK 2.0 常见问题"
---

<Note>
SDK **2.0.0**。鉴权：[授权与许可证](/zh/intro/license)。枚举：[参数枚举](/zh/intro/makeup)。
</Note>

## 接入

### 引擎创建 / `init()` 失败

- `EngineConfig` 必须设置 `licenseToken`、`authProxyUrl`、`fetchAuthResponse` **之一**。Web 配置上没有 `appId` / `appKey` 字段。
- 生产环境：代理必须原样返回鉴权接口响应体（`{success, token}` 或错误 JSON）。见 [授权与许可证](/zh/intro/license)。
- 确认 `/resource.fbd`（或你的 `resourcePath`）返回 HTTP 200。
- 确认页面是 HTTPS 或 localhost。
- 查看 `FacebetterError.code`（[错误处理](/zh/web/error-handling)）。

### 怎么传 AppID / AppKey？

不要写在 `EngineConfig` 上。密钥放在服务器，用 `authProxyUrl`，或签发 `licenseToken`。仅 localhost：`createDirectAuthFetcher({ appId, appKey })`。

### 浏览器能用离线 `.lic` 吗？

不能。Web 必须走在线 v2 鉴权。

### 如何升级包？

```bash
npm install facebetter@2.0.0
```

## 效果

### 美颜太弱或过猛

强度范围 `[0.0, 1.0]`。美型范围 `[-1.0, 1.0]`（`0` 为关闭）。静图可试 `FrameType.Image`。风格枚举见 [参数枚举](/zh/intro/makeup)。

### 滤镜 / 贴纸没效果

把 `.fbd` 的**路径或字节**直接传给 `setFilter` / `setSticker`。空字符串会抛错 — 关闭请用 `clearFilter()` / `clearSticker()`。

### 虚拟背景是黑的 / 没变化

调用 `setVirtualBackgroundBlur(level)` 或 `setVirtualBackground(path|Uint8Array)`，不要传 options 对象。虚化传 `0` 会关闭效果。绿幕时 `setChromaKey` 只设遮罩，仍需设置虚化或背景图。

### 输出全黑

视频可能还没有帧（`videoWidth === 0`），或拿不到 Canvas 上下文。仅在 `engine.initialized` 之后调用 `processImage`。

## 性能

### 预览卡

使用 `FrameType.Video`，限制采集分辨率（例如 720p），不要开关键点回调，循环放在 `requestAnimationFrame` 里。可看 `engine.getStats().avgProcessTimeMs`。

### SPA 里内存一直涨

离开页面时调用 `engine.destroy()`。路由切换不要泄漏引擎实例。

## 兼容性

支持 Chrome 57+、Firefox 52+、Safari 11+、Edge 16+。移动端建议降低采集分辨率和参数强度。

## React 示例

```jsx
import { useEffect, useRef } from 'react';
import { BeautyEffectEngine, EngineConfig } from 'facebetter';

function BeautyPreview() {
  const engineRef = useRef(null);

  useEffect(() => {
    const engine = new BeautyEffectEngine(new EngineConfig({
      authProxyUrl: '/api/facebetter/auth',
    }));
    engineRef.current = engine;
    engine.init().catch(console.error);
    return () => {
      engine.destroy();
      engineRef.current = null;
    };
  }, []);

  return <canvas />;
}
```

完整示例在 SDK 仓库的 `demo/web/react`。

## 相关文档

- [快速开始](/zh/web/quick-start)
- [实现美颜](/zh/web/implement-beauty)
- [错误处理](/zh/web/error-handling)
- [最佳实践](/zh/web/best-practices)
- [API 参考](/zh/web/api-reference)
