---
title: "实现美颜"
description: "在 Windows 上用 C++ API 集成 Facebetter SDK 2.0"
---

<Note>
SDK **2.0.0**。头文件位于 `facebetter/*.h`。美妆 / 美型枚举见 [参数枚举](/zh/intro/makeup)。鉴权见 [授权与许可](/zh/intro/license)。
</Note>

Windows 通过 C++ 库接入（`facebetter.lib` + `facebetter.dll`）。流程参考 [C++ 桌面 Demo](https://github.com/pixpark/facebetter-sdk)（`demo/cpp`）。

## 引入头文件

```cpp
#include <facebetter/beauty_effect_engine.h>
#include <facebetter/beauty_params.h>
#include <facebetter/image_frame.h>
#include <facebetter/type_defines.h>

using namespace facebetter;
using namespace facebetter::beauty_params;
```

## 集成流程

```
SetLogConfig → Create(EngineConfig) → 设置效果 → ProcessImage → 渲染
```

没有「启用美颜类型」这一步。强度大于 `0` 即开启对应效果。

---

## 1. 配置日志（可选）

必须在 `Create` **之前**调用，才能看到初始化日志。

```cpp
LogConfig log_cfg;
log_cfg.console_enabled = true;
log_cfg.file_enabled = false;
log_cfg.level = LogLevel::Info;
BeautyEffectEngine::SetLogConfig(log_cfg);
```

级别由低到高：`Trace` / `Debug` / `Info` / `Warn` / `Error` / `Critical`。

---

## 2. 创建引擎

桌面端 **必须** 将 `resource_path` 设为 `resource.fbd` **文件**路径。

**鉴权优先级：** `license_token` 非空时，本地校验该 token（许可证 token、`{token}` JSON 或离线 `.lic`），不再用 `app_id` + `app_key` 联网。否则使用 `app_id` + `app_key` 请求 `/facebetter/v2/auth`。

```cpp
EngineConfig cfg;
cfg.app_id = "your_app_id";
cfg.app_key = "your_app_key";
cfg.resource_path = "resource/resource.fbd";  // 指向 .fbd 文件
cfg.external_context = false;                 // SDK 管理 OpenGL 上下文

// 可选：非空时优先
// cfg.license_token = "/* 许可证 token、{token} JSON 或 .lic 内容 */";

std::shared_ptr<BeautyEffectEngine> engine = BeautyEffectEngine::Create(cfg);
if (!engine) {
    // 检查 resource_path、license_token / app_id+app_key 以及日志
    return -1;
}
```

<Warning>
`resource_path` 必须是 `resource.fbd` 文件，不能是目录。相对路径相对进程工作目录解析。生产环境建议用绝对路径。
</Warning>

---

## 3. 设置美颜参数

皮肤 / 美妆强度为 `[0.0, 1.0]`，`0` 表示关闭。美型为 **`[-1.0, 1.0]`**。完整枚举见 [参数枚举](/zh/intro/makeup)。

### 皮肤

```cpp
engine->SetSmoothing(0.5f);
engine->SetSmoothingStyle(SmoothingStyle::Natural);
engine->SetWhitening(0.3f);
engine->SetWhiteningStyle(WhiteningStyle::ColdWhite);
engine->SetSharpening(0.2f);
engine->SetRosiness(0.2f);
engine->SetBeautySkinOnly(true);  // 仅作用于检测到的皮肤
```

### 美型

```cpp
engine->SetReshape(Reshape::FaceThin, 0.4f);
engine->SetReshape(Reshape::EyeSize, 0.3f);
engine->SetReshape(Reshape::Chin, -0.2f);  // 负值为相反方向
```

### 美妆

强度、形状、色号分开设置。口红只有色号；美瞳只有色号；修容只有形状。

```cpp
engine->SetLipstick(0.6f);
engine->SetLipstickColor(LipstickColor::Rouge);

engine->SetBlush(0.4f);
engine->SetBlushStyle(BlushStyle::SunKissed);
engine->SetBlushColor(BlushColor::CoralPink);

engine->SetContour(0.5f);
engine->SetContourStyle(ContourStyle::Natural);

engine->SetEyeShadow(0.4f);
engine->SetEyeShadowStyle(EyeShadowStyle::Soft);
engine->SetEyeShadowColor(EyeShadowColor::Plum);
```

眼线、眉毛、睫毛、美瞳同理（`SetEyeLiner` / `SetEyebrow` / `SetEyelash` / `SetPupil` 及对应 Style / Color）。

---

## 4. 滤镜、贴纸与虚拟背景

传入 **文件路径** 或 **编码后的字节**。

```cpp
engine->SetFilter("assets/filters/natural.fbd");
engine->SetFilterIntensity(0.8f);
engine->ClearFilter();

engine->SetSticker("assets/stickers/face/black_glass.fbd");
engine->ClearSticker();

engine->SetVirtualBackgroundBlur(0.6f);  // [0, 1]；0 清除填充
engine->SetVirtualBackground("assets/background.jpg");
engine->ClearVirtualBackground();

engine->SetChromaKey(ChromaKeyColor::Green);
engine->SetChromaKeySimilarity(0.4f);
engine->SetChromaKeySmoothness(0.1f);
engine->SetChromaKeyDesaturation(0.2f);
engine->ClearChromaKey();  // 回到人像分割蒙版
```

LUT / 贴纸纹理在下一次 `ProcessImage`（当前 GL 线程）时创建。

---

## 5. 回调与统计

```cpp
EngineCallbacks cbs;
cbs.on_engine_event = [](int code, const std::string& message) {
    // 0 授权成功，1 授权失败，100 初始化完成，101 初始化失败
    printf("[event] %d %s\n", code, message.c_str());
};
cbs.on_face_landmarks = [](const std::vector<FaceDetectionResult>& faces) {
    printf("faces=%zu\n", faces.size());
};
engine->SetCallbacks(cbs);

EngineStats stats = engine->GetStats();
printf("fps=%.1f avg=%.2fms session=%.1fs\n",
       stats.fps, stats.avg_process_time_ms, stats.session_time_s);
```

---

## 6. 处理图像

在 `ImageFrame::type` 上设置 `FrameType::Video`（实时，默认）或 `FrameType::Image`（单张照片）。

```cpp
auto input = ImageFrame::CreateWithRGBA(rgba_data, width, height, stride);
input->type = FrameType::Video;

auto output = engine->ProcessImage(input);
if (output && output->Data()) {
    const uint8_t* p = output->Data();
    int w = output->Width();
    int h = output->Height();
}
```

**从文件（照片模式）：**

```cpp
auto input = ImageFrame::CreateWithFile("input.jpg");
input->type = FrameType::Image;
auto output = engine->ProcessImage(input);
output->ToFile("output.jpg", 95);
```

<Tip>
引擎会尽量保持输入 / 输出格式一致（RGBA 进 RGBA 出，I420 进 I420 出）。需要其他布局时用 `Convert(Format::BGRA)`。
</Tip>

### 上传到 OpenGL（预览）

```cpp
GLuint tex = 0;
glGenTextures(1, &tex);
glBindTexture(GL_TEXTURE_2D, tex);
glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_LINEAR);
glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_LINEAR);

glTexImage2D(GL_TEXTURE_2D, 0, GL_RGBA,
             output->Width(), output->Height(),
             0, GL_RGBA, GL_UNSIGNED_BYTE, output->Data());
```

将 `facebetter.dll` 放在可执行文件同目录（Demo 的 CMake 构建后会自动复制）。

---

## 7. 最小示例

```cpp
LogConfig log_cfg;
log_cfg.console_enabled = true;
log_cfg.level = LogLevel::Info;
BeautyEffectEngine::SetLogConfig(log_cfg);

EngineConfig cfg;
cfg.app_id = "your_app_id";
cfg.app_key = "your_app_key";
cfg.resource_path = "resource/resource.fbd";
auto engine = BeautyEffectEngine::Create(cfg);
if (!engine) return -1;

engine->SetSmoothing(0.5f);
engine->SetReshape(Reshape::FaceThin, 0.3f);
engine->SetLipstick(0.5f);
engine->SetLipstickColor(LipstickColor::Rouge);

auto input = ImageFrame::CreateWithRGBA(rgba, width, height, stride);
input->type = FrameType::Video;
auto output = engine->ProcessImage(input);
```

使用完毕后释放 `shared_ptr`（`engine.reset()`）。`BeautyEffectEngine` **不是**单例。

接入 TRTC / 声网 / LiveKit：见 [第三方对接](/zh/windows/third-party-integration)。

## 相关文档

<CardGroup cols={2}>
  <Card title="第三方对接" href="/zh/windows/third-party-integration">TRTC / 声网 / LiveKit</Card>
  <Card title="API 参考" href="/zh/windows/api-reference">全部 C++ 方法与类型</Card>
  <Card title="错误处理" href="/zh/windows/error-handling">返回码与 DLL 问题</Card>
  <Card title="最佳实践" href="/zh/windows/best-practices">性能与线程</Card>
</CardGroup>
